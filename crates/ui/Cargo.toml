[package]
name = "tuneo-ui"
version = "0.1.0"
edition = "2024"

[dependencies]

[dependencies.slint]
version = "1.15"
default-features = false
features = [
  "accessibility",
  "compat-1-2",
  "backend-winit",
  "renderer-winit-skia",
]

[build-dependencies]
slint-build = "1.15"
