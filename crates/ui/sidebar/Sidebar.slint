import { ScrollView } from "std-widgets.slint";
import { SidebarTheme } from "Theme.slint";
import { SidebarButton, SidebarIcon } from "SidebarButton.slint";
import "../assets/fonts/Rubik/Rubik-Regular.ttf";

export component Sidebar {
    width: 220px;

    property <int> selected-index: 0;

    Rectangle {
        width: parent.width;
        height: parent.height;
        background: SidebarTheme.backgroundContainer;

        HorizontalLayout {
            alignment: center;

            VerticalLayout {
                alignment: start;
                spacing: 10px;

                Image {
                    source: @image-url("../assets/Logo.png");
                }

                menu-section := Rectangle {
                    width: 180px;
                    height: 40px;
                    HorizontalLayout {
                        alignment: start;
                        padding-left: 20px;

                        Text {
                            text: "Menu";
                            font-size: 16px;
                            font-family: SidebarTheme.sidebarTypoTitle;
                            color: SidebarTheme.sidebarSectionTitle;
                            vertical-alignment: center;
                        }
                    }
                }

                SidebarButton {
                    label: "Home";
                    icon-commands-default: SidebarTheme.homeIconDefault;
                    icon-commands-selected: SidebarTheme.homeIconSelected;
                }

                SidebarButton {
                    label: "Genres";
                    icon-commands-default: SidebarTheme.genresIconDefault;
                    icon-commands-selected: SidebarTheme.genresIconSelected;
                }

                SidebarButton {
                    label: "Stations";
                    icon-commands-default: SidebarTheme.stationsIconDefault;
                    icon-commands-selected: SidebarTheme.stationsIconSelected;
                }

                library-section := Rectangle {
                    width: 180px;
                    height: 40px;
                    HorizontalLayout {
                        alignment: start;
                        padding-left: 20px;

                        Text {
                            text: "Library";
                            font-size: 16px;
                            font-family: SidebarTheme.sidebarTypoTitle;
                            color: SidebarTheme.sidebarSectionTitle;
                            vertical-alignment: center;
                        }
                    }
                }

                SidebarButton {
                    label: "Recent";
                    icon-commands-default: SidebarTheme.recentIconDefault;
                    icon-commands-selected: SidebarTheme.recentIconSelected;
                }

                SidebarButton {
                    label: "Artist";
                    icon-commands-default: SidebarTheme.artistIconDefault;
                    icon-commands-selected: SidebarTheme.artistIconSelected;
                }

                SidebarButton {
                    label: "Albums";
                    icon-commands-default: SidebarTheme.albumIconDefault;
                    icon-commands-selected: SidebarTheme.albumIconSelected;
                }

                SidebarButton {
                    label: "Favorities";
                    icon-commands-default: SidebarTheme.favoritiesIconDefault;
                    icon-commands-selected: SidebarTheme.favoritiesIconSelected;
                }

                SidebarButton {
                    label: "local";
                    icon-commands-default: SidebarTheme.localIconDefault;
                    icon-commands-selected: SidebarTheme.localIconSelected;
                }

                playlist-section := Rectangle {
                    width: 180px;
                    height: 40px;
                    HorizontalLayout {
                        alignment: start;
                        padding-left: 20px;

                        Text {
                            text: "Playlist";
                            font-size: 16px;
                            font-family: SidebarTheme.sidebarTypoTitle;
                            color: SidebarTheme.sidebarSectionTitle;
                            vertical-alignment: center;
                        }
                    }
                }

                SidebarButton {
                    label: "Create new";
                    icon-commands-default: SidebarTheme.createIconDefault;
                    icon-commands-selected: SidebarTheme.createIconSelected;
                }

                ScrollView {
                    height: 120px;
                    viewport-height: 180px;
                    viewport-y: -40px;
                   
                    animate viewport-y {
                        duration: 1000ms;
                    }
                    Rectangle {
                        
                        width: parent.width;
                        height: parent.height;
                        background: blueviolet;
                        HorizontalLayout {
                            alignment: center;

                            VerticalLayout {
                                alignment: start;
                                SidebarButton {
                                    label: "";
                                    icon-commands-default: SidebarTheme.playlistIconDefault;
                                    icon-commands-selected: SidebarTheme.playlistIconSelected;
                                }

                                SidebarButton {
                                    label: "";
                                    icon-commands-default: SidebarTheme.playlistIconDefault;
                                    icon-commands-selected: SidebarTheme.playlistIconSelected;
                                }
                                SidebarButton {
                                    label: "";
                                    icon-commands-default: SidebarTheme.playlistIconDefault;
                                    icon-commands-selected: SidebarTheme.playlistIconSelected;
                                }
                                SidebarButton {
                                    label: "";
                                    icon-commands-default: SidebarTheme.playlistIconDefault;
                                    icon-commands-selected: SidebarTheme.playlistIconSelected;
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}
